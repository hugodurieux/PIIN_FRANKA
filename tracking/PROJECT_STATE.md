# Project State — PINN Franka Pipeline
_Last updated: 2026-07-22 (Stage 2/3 Milestone 2 CLOSED (Stage 3 effort-mode trajectory tracking empirically confirmed via numeric `/joint_states` capture; the "arm bit-for-bit frozen" bug's proposed cause, Hypothesis 3, was refuted with log + upstream-source evidence; root cause of the original freeze left deliberately unresolved, not blocking). SAME DAY, CONTINUED: Stage 4 (grasping) started, per user-specified "first version" target of Franka picking up an object in MuJoCo. First concrete gap closed: **the gripper now has real physics and is validated working end-to-end.** The hand/fingers in `panda_arm_mujoco.xml` were welded dead mass (no joints, no collision, no actuator) — given real prismatic joints, visual+collision geometry (including the palm, missed in the first pass and fixed after live testing showed the fingers moving but visually disconnected from the arm), an `<equality>` constraint coupling the two fingers, and a `<position>` actuator, retuned from an initially underdamped kp=500/no-kv (which overshot the joint's own [0,0.04] range on every open/close, visually crossing the fingers through each other) to kp=30/kv=3 (confirmed via live open/close test: `reached_goal: true`, `stalled: false`). Critically, `panda_finger_joint1/2` were added into the arm's EXISTING single `<ros2_control>` block rather than a separate one, after confirming via `mujoco_system_interface.cpp` source that a second `MujocoSystemInterface` instance would spin up a second, disconnected physics simulation. `panda_hand_controller` (already fully defined upstream, already expected by MoveIt2's own config) is now spawned and working. Remaining Stage 4 gaps: `stage4/gripper_controller.py` still only targets real hardware, not this project's now-working `panda_hand_controller` action; `_move_arm()` still stubbed; orchestration node doesn't exist. SAME DAY, CONTINUED AGAIN: the second concrete gap also closed — **scene environment (skybox/ground plane/lighting, previously "just Franka in the dark") and a first graspable object (a free-floating ~64g cube, `grasp_object`, resting at x=0.5m in front of the base) added directly into `panda_arm_mujoco.xml`, confirmed rendering and sitting correctly live.** SAME DAY, CONTINUED A THIRD TIME: the third concrete gap closed — **`MuJoCoGripperController` added to `stage4/gripper_controller.py`, wrapping `panda_hand_controller`'s `gripper_cmd` GripperCommand action into the project's `BaseGripperController` interface, validated live** (`homing()`/`open()`/`grasp()`/`read()` all confirmed working via a new manual test script, `stage4/test_mujoco_gripper.py`) after fixing three bugs found during that testing: a stale-`read()` bug (a zero-timeout `rclpy.spin_once` does not wait for new messages, even looped many times — needed a real nonzero-timeout spin window instead), a one-off discovery-timing `nan` (never reproduced, not chased further), and the test script's own pass/fail check silently treating NaN as passing (`nan < 0.075` is always `False` in Python). Note `grasp()`'s live test result is NOT yet proof of grasping the actual cube -- `_move_arm()` doesn't exist yet, so the arm's position during that test wasn't controlled. All Stage 2/3 MuJoCo-migration + Milestone 2 + gripper-joints + scene/object work is COMMITTED on branch `fix/effort-mode-actuator-lock` (not merged to main); this session's gripper-controller-class work is uncommitted as of this entry.)_

## 1. Objectives (from goal.md)
| # | Objective | Status | Evidence / where proven |
|---|-----------|--------|-------------------------|
| 1 | Automated "URDF-to-Model" Pipeline — accept any URDF, auto-extract kinematics via Pinocchio, generate network, produce trained dynamic model | in progress | `pinocchio_baseline/rnea_wrapper.py` (URDF -> RNEA); `network/grey_box_net.py` + `training/train.py` (architecture + training loop). N4 data-efficiency ablation flag (`--max_samples`) merged. **`pinocchio_baseline/panda.urdf` mass/inertia fix (2026-07-16, commit c179740):** the URDF shipped with zero `<inertial>` tags on any link; RNEA had been running on Pinocchio's placeholder inertias, not real Franka mass properties, for the project's entire prior history. Fixed with official `franka_description` values; discovered via first-ever comparison against an independently-massed physics engine (Isaac Sim). **Isaac Sim 6.0.1 data pipeline VALIDATED end-to-end** (`generate_isaac_dataset.py`, GPU machine): all 3 payloads (0/1/3 kg, ~49k samples each) generated with corrected URDF — **still the Stage 1 data source, unaffected by the Stage 2/3 simulator migration to MuJoCo this session.** A second data-quality issue (actuator-saturation-contaminated samples inflating the residual tail) was found and fixed — see Stage 1 section below. **First real (GPU) training runs DONE**: see `tracking/experiments_log.csv` — `isaac-multipayload-frictionnet-satfix` is the current reference baseline (val RMSE 0.80/0.88/0.90/0.54/0.37/0.36/0.30 Nm per joint). Li et al. (2025) payload-ID review and Li et al. (2025) compliant-PINN review both independently confirm dual payload-awareness (RNEA white-box + tau_res delta input) is ahead of the published art. **Independent cross-validation this session (2026-07-20):** the real Franka masses/inertias vendored into the new MuJoCo MJCF (`mujoco_menagerie`, from the same upstream `franka_description` source) match the values already fixed in `pinocchio_baseline/panda.urdf` — a second, independent confirmation that the earlier URDF mass fix was correct. |
| 2 | High-Frequency Real-Time Control at 1000 Hz (MPC integration, microsecond inference) | in progress | Stage 2: MoveIt2 motion planning working end-to-end against a simulated Franka Panda — **simulator migrated from Isaac Sim to MuJoCo this session (2026-07-20)**, Milestone 1 (Plan & Execute) re-validated and stable on the new stack; see Stage 2 section. Stage 3: `ComputedTorquePDController` wired into `pinn_controller_node.py`'s control loop since 2026-07-16, message contract updated this session for MuJoCo's `std_msgs/Float64MultiArray` topics (was Isaac's raw `sensor_msgs/JointState`); torque-computation logic itself unchanged (simulator-agnostic). **Not yet proven driven by the trained model** — Milestone 2 (routing MoveIt2's planned trajectory through `pinn_controller_node`/Stage 3 instead of MoveIt2's own default position-mode execution) is in progress, not yet complete. Inference-latency benchmark still not written. A live position-hold oscillation was found and fixed this session, traced to the inherited demo `ros2_control` loop running at 100 Hz rather than this project's documented 1000 Hz target — worth revisiting once Milestone 2 exercises the effort-control path at the target rate. **2026-07-21:** a 5th bug found in this same effort-mode path — `pinn_controller_node.py` was indexing measured joint state positionally instead of by name, scrambling q_meas against a 9-joint `/joint_states` topic (7 arm + 2 mock-gripper fingers, alphabetically sorted); fixed via name-based lookup (`_arm_indices`), human-verified holding stable through the position->effort switch, uncommitted on branch `fix/jointstate-name-indexing`. **2026-07-21 (continued, same session):** the previously-flagged `moveit_plan_bridge` planning failure (`error_code=99999`) is now RESOLVED/MOOT — it was a downstream symptom of the arm's corrupted start-state from bug #5, not an independent bug; it replanned successfully once retried after a clean pose reset. However, a NEW, DEEPER, still-UNRESOLVED bug was found: the controller publishes real, correctly time-varying commanded torque (not stuck/frozen/near-zero) during a tracking attempt, yet `/joint_states` measured position is bit-for-bit frozen (min==max to full float64 precision) across the entire tracking window, on all 7 arm joints. Five alternative hypotheses ruled out empirically this session (controller-manager state, MuJoCo-internal stale PID, viewer pause state, trajectory-staleness-window overlap, control-law/gain/interpolator bugs). **2026-07-22: Hypothesis 3 REFUTED.** The `ros2_control_node` startup-log diagnostic (identified 2026-07-21, run 2026-07-22) proved the plugin's mode switch is genuinely dynamic and correct — not locked at registration — confirmed independently by reading `mujoco_system_interface.cpp` from the plugin's actual upstream source. A clean relaunch (default `arm_control_mode:=both`, candidate fix NOT applied) was tried next and worked immediately: numeric `/joint_states` capture shows every arm joint moving substantially (0.2-1.15 rad) toward a commanded target over 8 seconds — real, decisive motion, qualitatively different from the old bug's exact bit-for-bit freeze. **Milestone 2 is now CLOSED — trajectory tracking is empirically confirmed working**, first time in this project's history. Root cause of the original freeze is not fully understood (H3 refuted, H4 — MuJoCo silent auto-reset-on-divergence — proposed but never tested since the clean retry simply worked) but is no longer blocking; left open deliberately per explicit user direction to stop digging once the check succeeded. |
| 3 | Scaling to 7-DoF — Augmented Lagrangian constraints + residual friction on a full Franka Panda | in progress | `training/constraints.py` implements AL penalties (torque limits + dissipativity) for all 7 joints. Stage 3 hard-clips to per-joint TORQUE_LIMITS. N2-Liu FrictionNet IMPLEMENTED (`network/friction_net.py`): structural dissipativity guarantee via Softplus-diagonal D matrix; hard guarantee `tau_friction · qdot <= 0` always; 6,087 params; physics PASSED. Hu et al. (2026) review passively validates that RNEA covers load-dependent terms and tau_res captures motion-state residuals. |
| 4 | Standardized Sim-to-Real Gap resolution via physics-constrained fine-tuning | in progress | `training/fine_tune.py` on branch `novelty/duong2024-N3-simtoreal-finetune` — PASSED physics validation. Loads pre-trained GreyBoxNet, freezes all layers except last 2 nn.Linear modules, fine-tunes under full PINN loss (MSE + AL). First code implementation of Goal 4. Real data pipeline (HDF5) not yet built. |

## 2. Pipeline architecture (URDF -> trained robot)

tau_pred = RNEA(q, qdot, qddot; URDF + delta) + GreyBoxNet([sin(q), cos(q), qdot, delta]) + FrictionNet([sin(q), cos(q), qdot, delta])
Loss     = MSE(tau_pred, tau_real) + AugmentedLagrangian(torque_limits, dissipativity)

FrictionNet is optional (--use_friction_net flag). When active, its structural guarantee absorbs part of the dissipativity constraint, so the AL lambda_dissip multiplier grows more slowly — intended and correct.

**Simulator split (2026-07-20):** Stage 1 (data generation, training) and Stage 2/3 (motion planning + control demo) now use two independent, unrelated simulators. Stage 1 continues to use Isaac Sim (`~/isaac-sim`, `generate_isaac_dataset.py`) purely as an offline data source for training — unaffected by anything below. Stage 2/3 now uses MuJoCo (`mujoco_ros2_control`) as the live "simulated hardware" the ROS2 control stack drives in real time — this is a Stage 2/3-only infrastructure change, not a Stage 1 change; the trained checkpoint (`models/run_20260716_121302/`) and all Stage 1 datasets are untouched and reused as-is by the Stage 3 controller.

### Stage 1 — PINN dynamics learning
Fully architected and pipeline validated. 6 CPU smoke-test runs completed (20 epochs, pre-URDF-fix, mechanics-only validation) plus **2 real GPU training runs (200 epochs, Isaac Sim data, corrected URDF)** — see `tracking/experiments_log.csv`. Current reference baseline: `isaac-multipayload-frictionnet-satfix` (`models/run_20260716_121302/`), best val loss 0.3995, per-joint val RMSE [0.80, 0.88, 0.90, 0.54, 0.37, 0.36, 0.30] Nm. **Untouched this session** — still Isaac Sim-based; see the simulator-split note above.

**Actuator-saturation data-quality fix (2026-07-16):** the first real training run (`isaac-multipayload-frictionnet-first-real`) trained successfully (best val loss 1.4767) but `controller/compute_error_bound.py` — written to get a real error bound for the Stage 3 Lyapunov gains — found per-joint *maximum* validation error of 91-110 Nm, exceeding the joint's own 87 Nm torque limit. Root cause: Isaac Sim's simulated actuator saturating at `maxEffort` under aggressive trajectory tracking; `tau_real` for those samples was the clamp value, not real dynamics, while `tau_theo` (computed from the *reference* acceleration) assumed the trajectory was actually achieved. The prior over-torque filter (`|tau| > limit`) didn't catch these since clamped samples sit exactly *at* the limit. Fixed via `SATURATION_MARGIN = 0.97` in `generate_isaac_dataset.py` (excludes samples within 3% of the limit); all 3 payload datasets regenerated (148,304 samples, down from 148,760) and retrained. Result: best val loss improved ~4x (1.4767 -> 0.3995), and the augmented-Lagrangian `dissip_viol` term changed from oscillating (never settling, up to 0.0657) to smoothly converging to 0.0006 — the contaminated samples were destabilizing training broadly, not just inflating a diagnostic tail. A smaller, distinct residual outlier population remains on joints 1-4 (p99.9 of 3-6 Nm, true max 26-65 Nm on ~0.09% of samples) attributed to transient PD-servo correction spikes at trajectory-segment starts, not saturation; this is now the basis for the Stage 3 Lyapunov error bound (below) via a p99.9-not-max methodology, documented as a deliberate weakening of the formal worst-case guarantee.

- White-box RNEA: `pinocchio_baseline/rnea_wrapper.py` — wraps `pinocchio.rnea`, handles payload injection into end-effector inertia, runs offline only. **`RneaBaseline.__init__` now uses `pin.buildReducedModel()`** (locks the two finger joints at neutral, reducing the Pinocchio model from nq=9 to nq=7) — required for the live 7-DoF controller (previously a Milestone-2-only bug, `ValueError: wrong argument size: expected 9, got 7`; fix carried into this session's Stage 2/3 work, unrelated to Stage 1 data generation which already used the equivalent fix in `generate_isaac_dataset.py`'s `_load_pin_model()`).
- Residual network: `network/grey_box_net.py` — MLP (default 4x256, Mish), input X in R^22 = [sin(q), cos(q), qdot, delta], output tau_res in R^7. ReLU forbidden.
- FrictionNet: `network/friction_net.py` — 2-layer Mish MLP (22->64->64->7), output D_diag = Softplus(d) + 1e-6, tau_friction = -D_diag * qdot. Structural guarantee: tau_friction · qdot = -sum(Softplus(d_i)*qdot_i^2) <= 0 always. 6,087 parameters. Exposes `forward(q, qdot, delta) -> tau_friction` and `forward_D(q, qdot, delta) -> D_diag`. (N2-Liu, synthesising N1-Duong + N1-SPEL)
- Physics constraints: `training/constraints.py` — `AugmentedLagrangian` class; enforces |tau_pred| <= TORQUE_LIMITS and tau_res · qdot <= 0 (dissipativity) with dual-ascent multiplier updates.
- Training loop: `training/train.py` — Adam optimizer, 90/10 train/val split, CSV logging, best-checkpoint save. Supports `--synthetic` (no Pinocchio needed) and real HDF5 datasets. Accepts `--max_samples N` (N4) and `--use_friction_net` (N2-Liu); when friction net active, logs D_diag mean per joint per epoch and saves `friction_net_best.pt` alongside `greybox_best.pt`.
- Sim-to-real fine-tuning: `training/fine_tune.py` — loads pre-trained checkpoint, freezes all layers except last 2 nn.Linear modules (4th hidden layer + output layer), fine-tunes for `--max_steps` gradient steps (default=100) under full PINN loss. Saves `greybox_finetuned.pt`. (N3-Duong)
- Dataset: `training/dataset.py` — `_random_subsample_indices()` helper; `FrankaDynamicsDataset` (single HDF5), `MultiPayloadDataset` (list of HDF5, concatenates tensors then subsamples combined pool), and `SyntheticDataset` all accept `max_samples` param (seed=42, before train/val split). `--data` CLI arg accepts one or more paths (multi-payload training validated 2026-06-26, 147,734 samples).
- Constants: `network/constants.py` — single source of truth for joint limits, velocity limits, input/output dims, payload values, `FRICTION_NET_HIDDEN = 64`.

Data pipeline:
- **Isaac Sim generator VALIDATED end-to-end** (`generate_isaac_dataset.py`) — PRIMARY training data source, Isaac Sim 6.0.1 (`isaacsim.*` namespace). Plays Fourier+Sobol trajectories inside Isaac Sim physics engine; tau_real from `get_measured_joint_efforts()` (Isaac Sim Franka USD friction/damping); tau_theo from Pinocchio RNEA (corrected panda.urdf). tau_res is non-zero and physically meaningful. Filters both over-torque and near-saturation samples (`SATURATION_MARGIN = 0.97`, see Stage 1 section above). Outputs `data/isaac_{payload}kg.h5` (same HDF5 schema). Run via Isaac Sim's bundled Python (`./python.sh generate_isaac_dataset.py --payload N`) from the Isaac Sim install directory — output lands in `<cwd>/data/`, must be moved to the project's `data/` directory before training. **Still the sole live data-generation path — the Stage 2/3 simulator migration to MuJoCo (this session) does not touch this file or this workflow.**
- **Fourier baseline** (`generate_fourier_dataset.py`) — SMOKE-TEST ONLY. tau_res = synthetic Coulomb+viscous (hand-tuned). Useful for CPU pipeline validation; does not produce paper-quality residuals. Outputs `data/fourier_baseline_{payload}kg.h5`.

### Stage 2 — Motion planning (MoveIt2 / ROS2)
**Simulator migrated from Isaac Sim to MuJoCo this session (2026-07-20), NOT YET COMMITTED to git.** Still targets a *simulated* Franka Panda, not real hardware.

- **Simulator swap, rationale:** replaced the Isaac Sim + NVIDIA `isaac_moveit` stack (which required cloning, initializing submodules, and building `~/IsaacSim-ros_workspaces/jazzy_ws` outside the repo) with **`mujoco_ros2_control`**, an apt-installable ROS2 Jazzy package (`ros-jazzy-mujoco-ros2-control`). This removes the external submodule/build dependency entirely for Stage 2/3 — no more `~/IsaacSim-ros_workspaces` needed to run MoveIt2 + the controller stack. Isaac Sim itself (`~/isaac-sim`) remains installed and in active use, but now exclusively for Stage 1 data generation; it is no longer part of the Stage 2/3 execution path. **2026-07-21 (this session): this decision is now under partial reconsideration** — `mujoco_ros2_control` is a compiled binary with no local source available for inspection/patching, which has become a real liability for debugging the actuator-mode-lock hypothesis below (see Milestone 2 update). This is a tradeoff to document explicitly in the paper's methodology/limitations section, not a reason to reverse the swap yet.
- **New `ros2_ws/src/pinn_franka_controller/mujoco/franka_emika_panda/`** (new, untracked): vendored Franka Panda MJCF (`panda_arm_mujoco.xml`) + mesh `assets/` + `LICENSE`, sourced from Google DeepMind's `mujoco_menagerie` (Apache-2.0). Patched: joints renamed `panda_joint1..7` to match this project's convention (was `mujoco_menagerie`'s own naming), the upstream position-servo actuators replaced with a single direct-torque `motor` actuator per joint (required for Stage 3 effort control to have authority — no competing built-in servo, unlike the Isaac Sim USD's high-stiffness default drive that had to be worked around in the reverted Milestone 2 attempt), adjacent-link self-collision excludes added to match `config/panda.srdf`'s curated list. **2026-07-21:** the Franka hand + two fingers were added to this MJCF as mass-only welded bodies to fix a static gravity mismatch (already reflected here from a prior debugging session).
- **New `ros2_ws/src/pinn_franka_controller/urdf/`** (new, untracked): `panda_mujoco.ros2_control.xacro`, `panda_mujoco.urdf.xacro`, `panda_mujoco_description.urdf` — a patched copy of `moveit_resources_panda_moveit_config`'s URDF with real Franka inertial data added. Needed because that upstream package is a kinematics-only demo config (zero mass/inertia on every link, fine for MoveIt2's own kinematic planning but insufficient for MuJoCo's dynamics compiler, unlike the old Isaac/`mock_components` path which never needed the URDF's own inertials). **2026-07-21 (continued session, speculative/untested): a candidate `arm_control_mode` argument (`both`/`effort`) was added to `panda_mujoco.ros2_control.xacro` and `panda_mujoco.urdf.xacro`** to try forcing pure effort-mode command-interface declaration per joint — see Milestone 2 update below. UNTESTED, do not treat as a fix until validated.
- **New `launch/mujoco_franka_moveit.launch.py`**: replaces the external `isaac_moveit.launch.py` dependency entirely — self-contained, no external workspace required. **2026-07-21 (continued session, speculative/untested): a new `arm_control_mode` launch argument was added**, forwarding to the xacro args above; in `effort` mode, MoveIt2's position-mode `panda_arm_controller` (Milestone 1's path) is not spawned at all, since no position command interface exists to claim in that mode. Mutually exclusive with `both` mode per launch, not simultaneous. UNTESTED.
- **New `config/mujoco_effort_controller.yaml`** (new `panda_effort_controller`, the Stage 3 effort-mode ros2_control controller) and **`config/mujoco_pid.yaml`** (internal PID gains letting MoveIt2's default position-mode `panda_arm_controller` drive the same single per-joint torque actuator that effort mode also uses — both controller modes share one underlying MJCF actuator).
- **`ros2_ws/src/pinn_franka_controller/pinn_franka_controller/moveit_plan_bridge.py`** — restored this session (originally written and validated during the prior session's reverted Milestone 2 attempt, then deleted by that revert; the code itself was simulator-agnostic and is unaffected by the MuJoCo migration). Raw `moveit_msgs/action/MoveGroup` action client, `plan_only=True`, republishes the planned trajectory to `/pinn_controller/desired_trajectory`. A pub/shutdown DDS discovery race (dropped trajectory messages on the 2nd+ invocation) was found and fixed in an earlier debugging session (spin_once + sleep before shutdown). **2026-07-21 (earlier this session): a `error_code=99999` planning failure was found for at least one tested target** — **UPDATE (2026-07-21, continued, same session): this is now RESOLVED/MOOT.** Once the joint-indexing fix (bug #5, below) was applied and a clean pose reset performed, `moveit_plan_bridge` replanned successfully on retry. The `error_code=99999` was therefore a downstream symptom of the arm's corrupted start-state under the earlier bug, not an independent bug in `moveit_plan_bridge` itself. No longer tracked as an open item.
- **`simulation/isaac_franka_moveit_bridge.py`** — **kept in the repo as deprecated/historical reference, NOT deleted.** No longer part of the active Stage 2/3 workflow; superseded by the MuJoCo stack above. Left in place since it documents the Isaac Sim ROS2-bridge OmniGraph pattern and remains directly relevant to Stage 1's continued Isaac Sim usage as a reference (even though Stage 1's own data-generation script does not use ROS2 topics).
- **Milestone 1 — RE-VALIDATED and STABLE on the new MuJoCo stack this session:** MoveIt2 Plan & Execute in RViz correctly drives the simulated Franka Panda; RViz and MuJoCo both render the robot correctly and stay in agreement. A live position-hold oscillation was found and fixed during validation: traced to the `ros2_control` control loop inherited from the upstream MoveIt2 demo config running at 100 Hz (a rate meant for fake/mock hardware demos), not this project's documented 1000 Hz target — confirmed stable after the fix. This supersedes the prior session's Isaac-Sim-based Milestone 1 result, which is now historical (the underlying achievement — MoveIt2 driving a simulated Franka end-to-end — carries over, only the simulator changed). **Note: Milestone 1 is only spawnable in the default `arm_control_mode:=both` launch configuration** — the untested `effort`-only mode (2026-07-21) would remove this path from that launch, by design.
- **Milestone 2 — IN PROGRESS, NOT YET CLOSED.** Goal unchanged: route MoveIt2's planned trajectory through `pinn_controller_node` so that Stage 3's trained-model-driven torque control — using the existing reference checkpoint `models/run_20260716_121302/` — actually drives the simulated robot, instead of MoveIt2's default position-mode `panda_arm_controller` execution path.
- **Milestone 2 update (2026-07-21, earlier this session):** a 5th bug (positional joint-state slicing, see Stage 3 section below) was found and fixed on branch `fix/jointstate-name-indexing`, and the arm now holds cleanly through the position->effort controller switch (human-verified in the MuJoCo viewer).
- **Milestone 2 update (2026-07-21, CONTINUED this session) — trajectory tracking attempted and found broken:** with the arm holding cleanly and `moveit_plan_bridge` replanning successfully (see above), this session attempted the first real trajectory-tracking test (arm following a *moving* target, not just holding). Result: the controller publishes real, substantial, correctly time-varying commanded torque (e.g. joint1 ranged ~0 to 38.65 Nm, joint4 ranged 22.17 to 29.44 Nm over the tracking window), but the arm's measured `/joint_states` position did not change AT ALL: confirmed to full float64 precision (min==max exactly) across a 4-second capture window. Five alternative hypotheses were ruled out via live empirical tests (see Objective 2 row / Section 3c). Leading hypothesis at the time (H3): the compiled `mujoco_ros2_control` plugin binary might lock each joint's motor actuator into position-PID mode with effort disabled at registration, independent of ROS2-controller-manager state.
- **Milestone 2 update (2026-07-22) — Hypothesis 3 REFUTED, trajectory tracking CONFIRMED WORKING:** ran the diagnostic identified the day before (grep the exact session's `ros2_control_node` log for "control enabled" strings). Result: the log shows a single clean position→effort switch that correctly persisted (as `effort control enabled`) through a `reset_world` call and a full `unload` of `panda_arm_controller`, all the way to session end — H3 refuted. Independently confirmed by reading `mujoco_system_interface.cpp` from `github.com/ros-controls/mujoco_ros2_control` directly: `perform_command_mode_switch()`/`write()` implement correct dynamic per-cycle switching, no lock-at-registration mechanism exists in the source. Comments in `panda_mujoco.ros2_control.xacro` and `mujoco_franka_moveit.launch.py` asserting H3 as confirmed fact (written before the diagnostic ran) were corrected. A new Hypothesis 4 (MuJoCo's built-in silent auto-reset-on-divergence, which could reset `mjData` without pausing `sim_->run` or logging via RCLCPP) was proposed but never tested — a clean relaunch (default `arm_control_mode:=both`, candidate fix NOT applied) was tried instead and worked immediately. Two numeric `/joint_states` captures were taken: the first (re-sending an already-reached target) showed only ~1e-9 to 1e-8 rad of PD-holding jitter (inconclusive, no transition inside the window); the second, properly bracketing a fresh move, is decisive — every joint moved 0.2-1.15 rad, consistently converging toward the new target over 8 seconds.
- **Milestone 2 status: CLOSED (2026-07-22).** Trajectory tracking is now empirically confirmed working — the first time in this project's history. Root cause of the original freeze remains not fully understood (left open deliberately, not blocking); if it recurs, H4 and the untested `arm_control_mode:=effort` candidate fix (still present on this branch, comments now accurate) are the next things to check.
- `trajectory_interpolator.py`: linear interpolation of (q_des, qdot_des, qddot_des) from JointTrajectory messages — unchanged this session, simulator-agnostic; re-reviewed 2026-07-21 (continued), still found correct.
- `launch/pinn_controller.launch.py` and `config/controller_params.yaml` — unchanged this session (last touched 2026-07-16); the new `launch/mujoco_franka_moveit.launch.py` is the file that changed for the simulator swap (and, speculatively, for the untested `arm_control_mode` argument).

### Stage 3 — Controller + integration
`ComputedTorquePDController` (fully implemented and tested since the earlier `stage3/computed-torque-pd-controller` merge) remains wired into the ROS2 node (since 2026-07-16); this session updated its I/O contract to match the new MuJoCo simulator, **NOT YET COMMITTED**.

- `controller/model_loader.py`: loads `GreyBoxNet` checkpoint from `.pt` + `config.json`. Unchanged this session. Still loads the same reference checkpoint (`models/run_20260716_121302/`) — untouched by the simulator migration.
- `controller/lyapunov_gains.py`: N3-Liu — computes `Kd = safety_margin * error_bound`, `Kp = Kd^2 / 4` (critical damping). `DEFAULT_ERROR_BOUND = [5.20, 5.66, 3.06, 3.80, 2.10, 2.38, 1.57]` Nm, recomputed 2026-07-16 from real validation data. `MANUAL_ERROR_BOUND = [5,5,5,5,2,2,2]` Nm + `MANUAL_PD_KP`/`MANUAL_PD_KD` (safety_margin=1.0) fallback gains added 2026-07-16, used when the node's `use_lyapunov_gains` parameter is `False`. Unchanged this session; re-reviewed 2026-07-21 (continued) as part of ruling out control-law bugs for the trajectory-tracking-frozen investigation — gain magnitudes confirmed substantial (e.g. joint1 Kp ~= 27 Nm/rad), not negligible; not the cause.
- `controller/compute_error_bound.py`: reconstructs the exact train/val split used by `training/train.py`, evaluates a checkpoint (GreyBoxNet + optional FrictionNet) on the validation set, and reports per-joint RMSE, percentiles (p50/p90/p99/p99.9/max), and worst-5-samples-per-joint. Unchanged this session.
- `controller/computed_torque_pd.py`: `ComputedTorquePDController.step()` — `tau = RNEA(q, qdot, qddot_des) + tau_res(q, qdot, delta) + Kp*(q_des - q) + Kd*(qdot_des - qdot)`, hard-clipped to TORQUE_LIMITS; all I/O numpy; `torch.no_grad()` for inference. `update_payload(delta)` updates delta at runtime (e.g. after grasping). Unchanged this session — simulator-agnostic, only consumes/produces plain numpy arrays. Code-reviewed this session and found logically correct (no bugs); re-confirmed correct again this session (continued) during the trajectory-tracking-frozen investigation — the controller demonstrably computes and publishes correct, substantial, time-varying torque, so the freeze is not in this class. This narrows the frozen-motion bug to somewhere between the published `/panda_effort_controller/commands` topic and MuJoCo's actual actuator application — i.e. `ros2_control`/`mujoco_ros2_control` infrastructure, not this project's own control-law code.
- `controller/payload_identification.py`: still a TODO stub, unchanged this session.
- **`pinocchio_baseline/rnea_wrapper.py` — `RneaBaseline.__init__` fixed this session:** now calls `pin.buildReducedModel()` to lock the two finger joints at neutral (nq 9 -> 7), matching the pattern already used in `generate_isaac_dataset.py`. This was a real, previously-latent bug (found during the prior session's reverted Isaac Sim Milestone 2 attempt, `ValueError: wrong argument size: expected 9, got 7` on the first live RNEA call) that would have recurred on any live 7-DoF controller run regardless of simulator; now fixed ahead of exercising Milestone 2 on MuJoCo.
- **`ros2_ws/src/pinn_franka_controller/pinn_franka_controller/pinn_controller_node.py` — message/topic contract changed this session:** switched from Isaac Sim's raw `sensor_msgs/JointState` on `isaac_joint_commands` (effort field) / `isaac_joint_states` (subscribed) to the standard ROS2-control `std_msgs/Float64MultiArray` on `/panda_effort_controller/commands` (published) / `joint_states` (subscribed) — the conventional `mujoco_ros2_control` / `ros2_control` topic contract. **All torque-computation logic is unchanged** — the node was already simulator-agnostic at the control-math level; only the transport-layer message type and topic names changed.
- **Bug #5 found and fixed this session (2026-07-21) — blind positional joint-state slicing:** `pinn_controller_node.py` extracted measured joint state via `self._joint_state.position[:7]` / `velocity[:7]`, assuming the incoming `/joint_states` message's first 7 entries were the 7 arm joints in `panda_joint1..7` order. In fact the live topic carries 9 joints — the 7 arm joints plus `panda_finger_joint1`/`panda_finger_joint2` from a mock gripper system sharing the same controller manager — and `joint_state_broadcaster` publishes them sorted alphabetically, which places the two finger joints before `panda_joint1`/`panda_joint2` and pushes `panda_joint6`/`panda_joint7` out of the first 7 slots entirely. RNEA (and the residual network) were therefore fed a scrambled 7-vector on every control tick, in both the main control loop and the gravity-compensation safety fallback. Symptom: the arm fell/collapsed 100% of the time immediately on switching from position-mode `panda_arm_controller` to effort-mode `panda_effort_controller`. Root-caused via live empirical testing (ruling out three other hypotheses: controller-switch sequencing, MuJoCo's internal PID fighting the effort command, ForwardCommandController command-buffer reset on activation) plus reading upstream `mujoco_ros2_control`/`ros2_controllers` source, then confirmed by directly inspecting a live `/joint_states` message. **Fix:** added a name-based index-lookup helper (`_arm_indices`) and replaced both `[:7]` slice sites with it. **Status: fix applied and human-verified working** — the arm now holds cleanly through the position->effort switch, directly observed in the MuJoCo viewer — **but lives on a new, uncommitted branch `fix/jointstate-name-indexing`, not yet merged to main.**
- **Bug found 2026-07-21, RESOLVED (not a bug) 2026-07-22 — the "trajectory tracking produces zero motion despite correct torque commands" symptom:** with bug #5 fixed and the arm holding cleanly, the first real trajectory-tracking test showed the controller publishing correct, substantial, time-varying commanded torque while `/joint_states` measured position stayed bit-for-bit frozen. `ComputedTorquePDController`, `TrajectoryInterpolator`, and `lyapunov_gains.py` were all re-reviewed and confirmed correct, ruling out this project's own control-law code. The leading hypothesis (H3, a `mujoco_ros2_control` actuator-mode lock) was refuted 2026-07-22 by log + upstream-source evidence (see Stage 2 Milestone 2 update above); a clean relaunch then produced real, numerically-confirmed motion on the first retry. Root cause of the original freeze itself is not fully understood, but the symptom no longer reproduces and Milestone 2 is closed.
- **Files touched across this work, COMMITTED 2026-07-22 on branch `fix/effort-mode-actuator-lock`** (the previously-planned `fix/jointstate-name-indexing` branch was never given separate commits — both branch names pointed at the same commit throughout, so this was committed as a clean series on the current branch instead): `pinocchio_baseline/rnea_wrapper.py`, `ros2_ws/src/pinn_franka_controller/package.xml`, `ros2_ws/src/pinn_franka_controller/pinn_franka_controller/pinn_controller_node.py`, `ros2_ws/src/pinn_franka_controller/setup.py`, `simulation/isaac_franka_moveit_bridge.py` (retained but unused), `ros2_ws/src/pinn_franka_controller/config/mujoco_effort_controller.yaml`, `ros2_ws/src/pinn_franka_controller/config/mujoco_pid.yaml`, `ros2_ws/src/pinn_franka_controller/launch/mujoco_franka_moveit.launch.py`, `ros2_ws/src/pinn_franka_controller/mujoco/`, `ros2_ws/src/pinn_franka_controller/pinn_franka_controller/moveit_plan_bridge.py`, `ros2_ws/src/pinn_franka_controller/urdf/` (including the `arm_control_mode` argument, comments corrected 2026-07-22 to remove the refuted H3-as-fact claim). Not merged to main — awaiting user review.

### Stage 4 — Grasping (optional)
Scaffolded and merged to main (commit b86f335). Fully isolated — zero modifications to Stages 1-3.

- `stage4/grasp_config.py`: `GraspConfig` dataclass — all grasping parameters in one place: grasp_width/force/speed, epsilon_inner/outer, open_width/speed, pre_approach_height/speed, lift_height/speed, max_torque_error, gripper_timeout, arm_motion_timeout, object_mass.
- `stage4/gripper_controller.py`: `BaseGripperController` ABC + `FrankaROS2GripperController` (real hardware, ROS2 Humble, franka_gripper Grasp/Move/Homing action clients) + `MuJoCoGripperController` (this project's own MuJoCo/ros2_control stack, `panda_hand_controller`'s `gripper_cmd` GripperCommand action, added 2026-07-22, validated live) + `MockGripperController` (no-hardware testing).
- `stage4/grasp_executor.py`: `GraspExecutor` state machine — `pick()` and `place()` methods; phases IDLE -> PRE_APPROACH -> APPROACH -> GRASPING -> LIFTING -> HOLDING -> RELEASING -> ABORTED. After successful grasp calls `arm.update_payload(object_mass)` to update Stage 3 controller payload estimate.
- `stage4/demo_targets.py`: named poses (box_center, box_left, box_right, place_tray, home); orientations TOP_DOWN and TILTED_30; `get_target(name)` returns (4,4) homogeneous transform in Franka base frame.
- `stage4/dry_run.py`: 6 offline tests (successful pick+place, gripper failure, abort, GraspConfig defaults, demo_targets validation, MockGripperController transitions) — all pass without hardware / ROS2 / trained model. Run with: `python -m stage4.dry_run`.

**Progress note (2026-07-22): Milestone 2 is now CLOSED** (see Stage 2/3 above) — the blocker that had held up `_move_arm()` since 2026-07-16 is cleared. Stage 4 is now the next body of work, per explicit user direction: "first version" = Franka picking up an object in MuJoCo (full 4-novelty thesis scope is the eventual "final version", tracked separately).

**Gripper joints — DONE, validated 2026-07-22 (first concrete Stage 4 gap closed):**
- `panda_arm_mujoco.xml`: the previously-welded hand/fingers now have real physics — prismatic joints (`panda_finger_joint1/2`, values from `mujoco_menagerie`'s WITH-hand `panda.xml`), visual + collision geometry for both the fingers (including 5 fingertip pad collision boxes each, for stable grasp contact) and the palm (`panda_hand` body — missed in the first pass, found live: fingers moved correctly but nothing visually connected them to the arm, fixed by adding the `hand_0..4.obj`/`hand.stl` meshes that were already vendored on disk but never referenced), an `<equality>` constraint keeping finger2 synced to finger1, and a `<position>` actuator on finger1 (retuned live from an initially underdamped `kp=500`/no `kv` — which overshot the joint's own `[0, 0.04]` range on every open/close, visible as the fingers crossing through each other — to `kp=30`/`kv=3`, confirmed via the real `panda_hand_controller` action: `reached_goal: true, stalled: false` for both open and close).
- **Critical architectural constraint, confirmed by reading `mujoco_system_interface.cpp` from `github.com/ros-controls/mujoco_ros2_control` before writing any xacro:** `MujocoSystemInterface::on_init()` constructs a private `MujocoSimulation` per hardware-component instance, not a shared singleton. `panda_finger_joint1/2` were therefore added *inside* the arm's existing single `<ros2_control>` block (`panda_mujoco.ros2_control.xacro`, same "PandaMujocoSystem"), not as a separate hand-specific hardware component — a second one, even pointed at the same MJCF file, would have spun up a second, disconnected physics simulation with the fingers floating detached from the arm.
- `panda_mujoco.urdf.xacro`: the old separate `mock_components`-only hand system ("PandaHandMockSystem") removed entirely, now redundant.
- `launch/mujoco_franka_moveit.launch.py`: now spawns `panda_hand_controller` (`position_controllers/GripperActionController`) — already fully defined upstream in `moveit_resources_panda_moveit_config`'s `ros2_controllers.yaml` and already expected by MoveIt2's own `gripper_moveit_controllers.yaml` (both loaded unmodified), just never spawned before. MoveIt2's SRDF `hand` planning group and `open`/`close` named states were also already complete, untouched.
- New convenience scripts `ros2_ws/gripper_open.sh` / `gripper_close.sh` wrap the `GripperCommand` action call (written as scripts after the equivalent one-line `ros2 action send_goal` command got corrupted by terminal line-wrapping during paste).

**Scene environment + first graspable object — DONE, validated 2026-07-22 (second concrete Stage 4 gap closed):**
- `panda_arm_mujoco.xml`: added directly into the robot file (not a separate wrapping `scene.xml` — the `mujoco_ros2_control` hardware plugin's `mujoco_model` param takes exactly one file, and an `<include>`-based split wasn't judged worth the risk on an already-validated working model for a first version). `<visual>` (headlight, haze, camera azimuth/elevation), skybox + checkered groundplane `<texture>`/`<material>` assets, a directional light, and a `floor` plane geom — values copied from `urdf/panda_mujoco_scene_info.xml`, a generic, robot-agnostic scene wrapper this project vendored during the earlier, since-abandoned attempt at runtime URDF-to-MJCF conversion, never actually wired into the static-MJCF path this project actually uses until now.
- A first graspable object, `grasp_object`: a free-floating 4cm cube (`<freejoint/>`, density-based ~64g mass, explicit friction matching MuJoCo's own default, orange rgba), resting on the floor at x=0.5m directly in front of the robot base — well inside the Franka's ~0.855m reach.
- Confirmed live: ground plane, sky, and the cube all render and sit correctly in the scene (previously "just Franka in the dark").

**MuJoCo-native gripper controller class — DONE, validated 2026-07-22 (third concrete Stage 4 gap closed):**
- `stage4/gripper_controller.py`: added `MuJoCoGripperController`, wrapping `panda_hand_controller`'s `gripper_cmd` `GripperCommand` action into the project's `BaseGripperController` interface. `width` (total finger separation, matching `HARDWARE_MAX_WIDTH=0.08`) is halved before sending as the action's `position` field (which only drives `panda_finger_joint1` directly) and doubled back when reading results. `grasp()` disambiguates the action's inherently ambiguous "stalled" result (means success against a real object, meant failure during the earlier kp=500 mis-tuning) by checking whether the final width falls within the requested epsilon tolerance band, the same way the real `franka_gripper` Grasp action does, rather than trusting "stalled" alone.
- New manual live test, `stage4/test_mujoco_gripper.py` (homing → grasp → open against the live ROS2 action; the existing `dry_run.py` is Mock-only and can't exercise this). Validated live after fixing 3 bugs found during testing: (1) `read()` returning stale data immediately after an action completed — a zero-timeout `rclpy.spin_once` does not wait for new messages at all, even called in a tight loop, since it only picks up whatever's already queued at that exact instant; fixed with a real nonzero-timeout spin window (`spin_once(timeout_sec=0.02)` across ~100ms, matching `/joint_states`' ~500 Hz rate); (2) a one-off all-NaN reading, likely a discovery-timing fluke on a freshly-started node, never reproduced, not chased further; (3) the test script's own pass/fail check silently treating NaN as passing (`nan < 0.075` is always `False` in Python) — fixed by checking `math.isnan()` explicitly, and by trusting the action's own `reached_goal` result as the authoritative pass/fail signal rather than a secondary, timing-sensitive `read()`.
- Note: `grasp()`'s live test result is NOT yet proof of grasping the actual cube — `_move_arm()` doesn't exist yet, so the arm's position during that test was wherever it was last left, not verified to be at `grasp_object`.

**Remaining Stage 4 gaps (not yet started), in dependency order:**
- `_move_arm()` in `grasp_executor.py` raises `NotImplementedError`; needs IK (Pinocchio or MoveIt2) + trajectory generation (Stage 2) + 1 kHz execution via Stage 3 (now unblocked). `TODO(stage4-arm-motion)` marks the integration point. **Next concrete step.**
- Stage 4 ROS2 orchestration node (joint-state subscriber + full grasp sequence) doesn't exist yet — only the offline, no-ROS2, no-sim `stage4/dry_run.py` exists.
- Final step once the above land: live integration test of the full pick sequence in MuJoCo (plan approach → move → close gripper on `grasp_object` → verify grasp → lift → place) — the first point where `grasp()`'s success can be verified against the real cube.

## 3. Implemented novelties
| ID | From paper | Description | Goal served | Branch | Validated | Merged |
|----|-----------|-------------|-------------|--------|-----------|--------|
| N1-Djeumou | Djeumou et al. (2022) | Compositional grey-box structure: known analytical terms (RNEA) composed with neural residual. REJECT — already fully implemented as the project's core architecture in `grey_box_net.py`. Architecture corroborated by peer-reviewed paper. | Goal 1, Goal 3 | main | architecture match confirmed | n/a (pre-existing) |
| N2-Djeumou | Djeumou et al. (2022) | Augmented Lagrangian training with Lagrange multiplier dual-ascent for hard physics constraints. REJECT — already fully implemented in `constraints.py`. | Goal 3 | main | architecture match confirmed | n/a (pre-existing) |
| N3-Djeumou | Djeumou et al. (2022) | Semi-supervised constraint enforcement on unlabeled joint-space points to extend dissipativity guarantees beyond training trajectories. INVESTIGATE — ablation study needed. | Goal 3, Goal 4 | not started | not validated | not merged |
| N1-Liu | Liu et al. (2024) | RK4 rollout loss — augment the training loss with a 4th-order Runge-Kutta forward simulation residual. REJECT — conflicts with RNEA grey-box architecture; would destroy primary novelty. | — | rejected | — | — |
| N2-Liu | Liu et al. (2024) | Cholesky dissipativity structural constraint — FrictionNet sub-module with Softplus-diagonal D matrix; tau_friction = -D_diag * qdot; hard structural guarantee tau_friction · qdot <= 0 always. Synthesises N2-Liu (Cholesky concept) + N1-Duong (L L^T algebraic kernel, diagonal case) + N1-SPEL (revolute-joint sparsity: diagonal D, not full 28-entry Cholesky). KEEP — IMPLEMENTED in `network/friction_net.py`; `--use_friction_net` flag in `training/train.py`; logs D_diag mean per joint; saves `friction_net_best.pt`. 6,087 parameters. Physics validator advisory: lambda_dissip grows more slowly when active (intended). | Goal 3 | `novelty/N2-Liu-frictionnet` | PASSED (physics validator) | **MERGED to main (commit ebc2ba3)** |
| N3-Liu | Liu et al. (2024) | Lyapunov stability template for Stage 3 controller — `Kd = safety_margin * error_bound`, `Kp = Kd^2/4` (critical damping), `DEFAULT_ERROR_BOUND = [5,5,5,5,2,2,2]` Nm placeholder. KEEP — implemented in `controller/lyapunov_gains.py`. DEFAULT_KP/DEFAULT_KD recomputed from real data (2026-07-16); a separate `MANUAL_PD_KP`/`MANUAL_PD_KD` fallback (fixed, safety_margin=1.0) was added for `use_lyapunov_gains=False` deployments — see Stage 3 section. | Goal 2 | `stage3/computed-torque-pd-controller` | PASSED (physics validator) | **MERGED to main** |
| N4-Liu | Liu et al. (2024) | `--max_samples` data-efficiency ablation flag — truncate training data to N random samples (seed=42, before train/val split) to quantify how few samples are needed vs. Liu's 25k-sample benchmark. KEEP — implemented. | Goal 1 | `novelty/liu2024-N4-max-samples` | PASSED (physics validator, non-blocking advisories) | **MERGED to main** |
| N1-Duong | Duong et al. (2024) | Cholesky-factored inverse mass sub-network — L L^T + eps*I algebraic trick (28 lower-triangular entries for 7x7 matrix, Softplus on diagonal) guarantees positive-definiteness. INVESTIGATE — algebraic input to N2-Liu FrictionNet design (diagonal case used). Folds into N2-Liu; implemented there. | Goal 3 | folds into N2-Liu | folded into N2-Liu PASSED | not separate |
| N2-Duong | Duong et al. (2024) | Separate dissipation sub-network. REJECT — duplicate of N1-Duong's algebraic core; no independent contribution beyond what N1-Duong and N2-Liu already capture. | — | rejected | — | — |
| N3-Duong | Duong et al. (2024) | 100-step payload fine-tuning protocol — freeze all layers except last 2 nn.Linear modules (4th hidden layer + output layer), fine-tune for `--max_steps` steps (default 100, range 50–200) under full PINN loss (MSE + AL), lr=1e-4 to limit catastrophic forgetting. KEEP — IMPLEMENTED in `training/fine_tune.py`. First code realisation of Goal 4. | Goal 4 | `novelty/duong2024-N3-simtoreal-finetune` | PASSED (physics validator) | **MERGED to main (commit 0aa4fdc)** |
| N1-WangCAC | Wang et al. (CAC 2024) | Sobol quasi-random data sampling — 10 Sobol-sampled q_center configurations per payload in `generate_fourier_dataset.py`; scipy.stats.qmc.Sobol, 7-D joint space, scrambled, per-payload seed. IMPLEMENTED — validated 2026-06-26. | Goal 1 | main | confirmed working | merged (in generate_fourier_dataset.py) |
| N2-WangCAC | Wang et al. (CAC 2024) | Trapezoidal physics residual in PINN loss — time-discretised integration of dynamics residual at 20 Hz. REJECT — conflicts with RNEA offline-qddot path (same fundamental problem as N1-Liu); 20 Hz discretisation incompatible with 1 kHz target. | — | rejected | — | — |
| N3-WangCAC | Wang et al. (CAC 2024) | EKF noise filter at PINN-NMPC interface — Extended Kalman Filter to denoise joint state estimates before feeding the PINN. REJECT — matrix inversion per control step is incompatible with sub-ms 1 kHz loop latency requirement. | — | rejected | — | — |
| N1-SPEL | Wang et al. (Scientific Reports 2025) | Physics-driven sparsity masks on friction matrix — CONFIRMED 2026-06-26 via `pinocchio_baseline/friction_sparsity_analysis.py`: all 7 Franka joints are JointModelRZ (independent revolute), URDF friction/damping all zero, 80% parameter reduction (7 vs 28 params). Diagonal D physically justified. Folds into N2-Liu FrictionNet. | Goal 3 | folds into N2-Liu | CONFIRMED via Pinocchio analysis | not separate |
| N2-SPEL | Wang et al. (Scientific Reports 2025) | URDF inertia constants as trainable scalar parameters — make Pinocchio URDF link inertias learnable during training. REJECT — violates RNEA-intact invariant; making them learnable weakens the white-box guarantee that is central to Goal 1. | — | rejected | — | — |
| N3-SPEL | Wang et al. (Scientific Reports 2025) | KAN (Kolmogorov-Arnold Network) activations replacing MLP layers. REJECT — violates Mish/Softplus-only activation constraint; also demonstrates 14% overfitting on real hardware data. | — | rejected | — | — |
| N1-WhenPhysics | Prabhakar et al. (ICLR 2026) | Equality/inequality constraint scheduling — annealing the strength of physics constraints over training epochs. REJECT — already the exact structure in `training/constraints.py` (dual-ascent augmented Lagrangian). | — | rejected | — | — |
| N2-WhenPhysics | Prabhakar et al. (ICLR 2026) | EMA momentum-smoothed per-residual loss balancing (beta=0.95). INVESTIGATE — per-joint val RMSE diagnostic LIVE in `training/train.py` (printed at end of every run). Fourier baseline result 2026-06-26: ratio 1.0× (joints 1-4: 0.239 Nm, 5-7: 0.247 Nm) — no imbalance on synthetic residual. Revisit after real robot recordings. | Goal 3 | diagnostic in main | confirmed no imbalance on Fourier data | diagnostic merged |
| N3-WhenPhysics | Prabhakar et al. (ICLR 2026) | UDE frozen-backbone fine-tuning — Universal Differential Equation approach with frozen physics backbone. REJECT — duplicate of N3-Duong already implemented in `training/fine_tune.py`. | — | rejected | — | — |
| N1-BacklashPINN | Hu et al. (IEEE/ASME T-Mech 2026) | Amplitude-weighted training loss — weight loss by motion amplitude to emphasise high-excitation segments when backlash is dominant. REJECT — redundant with N2-WhenPhysics EMA balancing (same motivation, same mechanism); also validated only on SIASUN/ROKAE 6-DoF with ReLU activations (forbidden). | — | rejected | — | — |
| N2-BacklashPINN | Hu et al. (IEEE/ASME T-Mech 2026) | Gaussian basis-function output head — replace final linear layer with a Gaussian RBF layer to localise backlash responses. REJECT — requires labeled backlash windows (unavailable for Franka); ReLU used throughout (forbidden activation). | — | rejected | — | — |
| N3-BacklashPINN | Hu et al. (IEEE/ASME T-Mech 2026) | TCN (Temporal Convolutional Network) sliding-window inference — replace MLP with causal TCN to capture hysteresis history. REJECT — stateful sliding window breaks the stateless 1 kHz control loop; incompatible with the project's real-time constraint. | — | rejected | — | — |
| N1-PayloadPINN | Li et al. (IEEE RA-L 2025) | QDD current-based payload identification — estimate payload inertia from quasi-direct-drive motor currents, no torque sensor needed. REJECT — Franka uses gearboxes (not QDD), making current-to-torque mapping nonlinear and unreliable; architecture mismatch. | — | rejected | — | — |
| N2-PayloadPINN | Li et al. (IEEE RA-L 2025) | Virtual payload link injection in Pinocchio for white-box payload awareness. REJECT — ALREADY FULLY IMPLEMENTED: `pinocchio_baseline/rnea_wrapper.py` uses `_inject_payload()` / `_restore_payload()` methods that modify the end-effector link inertia in Pinocchio before each RNEA call. This project's implementation predates and exceeds the paper's. | — | n/a (pre-existing) | architecture match confirmed | n/a (pre-existing) |
| N3-PayloadPINN | Li et al. (IEEE RA-L 2025) | PINN-inside-NMPC at 100 Hz — embed the trained PINN as a forward model inside a nonlinear MPC predict-and-optimise loop at 100 Hz. REJECT — CLAUDE.md mandates Stage 2 uses standard MoveIt2; NMPC replaces rather than extends MoveIt2, and 100 Hz is below the 1 kHz target. | — | rejected | — | — |
| N1-CompliantPINN | Li et al. (2025) — compliant manipulators | DeLaN-FFNN grey-box with explicit compliance terms (spring/damper contact model appended to RNEA). REJECT — harmful for Franka rigid-body dynamics: adding compliance terms to a rigid-body RNEA introduces model error, not model improvement; Franka has harmonic reducers, not compliant joints. Paper valuable as Related Work citation confirming independent validation of the grey-box DeLaN approach. | — | rejected | — | — |
| N2-CompliantPINN | Li et al. (2025) — compliant manipulators | Payload-weighted loss scaling (scale loss by estimated payload effect per joint). REJECT — weaker duplicate of N2-WhenPhysics EMA diagnostic; N2-WhenPhysics per-joint RMSE diagnostic already implemented in `training/train.py` with exponential smoothing option; no new mechanism. | — | rejected | — | — |
| N1-PIKANs | Toscano et al. (2025) | PIKAN (Physics-Informed Kolmogorov-Arnold Network) — replace MLP layers with B-spline/wavelet KAN layers throughout the PINN architecture. REJECT — KAN activations violate CLAUDE.md Mish/Softplus-only smoothness constraint; duplicate of N3-SPEL rejection reason. Toscano et al. survey broadly corroborates existing design choices (grey-box, AL constraints, frozen-backbone fine-tuning). | — | rejected | — | — |
| N2-SNRDiag | Toscano et al. (2025) | Spectral normalisation of residual diagonal (SNR) — apply spectral normalisation to the output layer of the residual network to enforce Lipschitz continuity. REJECT — serves no goal.md objective; project's AL dissipativity constraint already enforces energy-consistent behaviour; adding spectral normalisation would complicate the gradient landscape without a clear physics motivation. | — | rejected | — | — |
| N1-DeLaN4EC | Lutter, Listmann, Peters (IROS 2019) | Energy coherence loss — joint training loss enforcing T + V = H (kinetic + potential = Hamiltonian) across the Lagrangian network during rollout. REJECT — requires explicit T/V output heads that are absent from the grey-box RNEA-residual architecture; adding them would require learning H(q, qdot) explicitly, breaking the RNEA-intact invariant (Goal 1). | — | rejected | — | — |
| N2-DeLaN4EC | Lutter, Listmann, Peters (IROS 2019) | Forward + inverse model consistency loss — enforce that the forward model (M(q)qddot = tau) and inverse model (tau_pred) agree on the same trajectory. REJECT — requires computing H^{-1}(q) at 7-DoF (7x7 matrix inversion per sample); computationally expensive and introduces the same conflict as N1-Liu (RK4 rollout) with the RNEA white-box path. | — | rejected | — | — |
| N3-DeLaN4EC | Lutter, Listmann, Peters (IROS 2019) | Parametric Stribeck friction model — augment the Lagrangian network output with a Stribeck curve parameterised by static friction, Coulomb friction, and Stribeck velocity (tanh/sign-based). REJECT — sign(qdot) is non-smooth and violates the Mish/Softplus smoothness constraint; Stribeck static-friction peak is not present in Franka's harmonic-reducer joints; FrictionNet (N2-Liu, MERGED) already provides a structurally superior smooth dissipation parameterisation. | — | rejected | — | — |
| N1-DeLaN | Lutter, Ritter, Peters (ICLR 2019) | Cholesky L(q)L(q)^T parameterisation for the full inertia matrix H(q) — learn a lower-triangular L(q) such that H = L L^T guarantees positive definiteness everywhere. REJECT — the project's RNEA (via Pinocchio) already provides H(q) analytically from the URDF, making a learned H superfluous; Cholesky concept already absorbed into N1-Duong and ultimately the diagonal FrictionNet (N2-Liu, MERGED). Seminal paper: original source for Cholesky+Softplus diagonal design pattern in physics-structured NNs. | — | rejected | — | — |
| N2-DeLaN | Lutter, Ritter, Peters (ICLR 2019) | Online in-loop model update at 200 Hz — re-run a few gradient steps on the Lagrangian network during closed-loop execution to adapt to changing conditions. REJECT — breaks the stateless 1 kHz control loop invariant (no gradient computation at runtime); N3-Duong offline fine-tuning (MERGED) already covers the model-update goal without runtime overhead. | — | rejected | — | — |
| N1-DiffNEA | Sutanto et al. (L4DC 2020) | Differentiable Newton-Euler Algorithm (DiffNEA) with learnable inertial parameters — make URDF link masses and inertias differentiable and jointly optimised with the neural residual. REJECT — violates the RNEA-intact invariant: making URDF parameters learnable destroys the white-box analytical guarantee that is the core of Goal 1 (automated URDF-to-model pipeline). | — | rejected | — | — |
| N2-DiffNEA | Sutanto et al. (L4DC 2020) | Per-joint theta^2 viscous damping term — add a quadratic velocity-dependent damping d_i * qdot_i^2 per joint in the RNEA output. REJECT — weaker variant of the existing FrictionNet (N2-Liu, MERGED): FrictionNet learns a full nonlinear D(q, qdot, delta) * qdot dissipation, which subsumes the fixed quadratic approximation; Sutanto's quadratic form also ignores payload conditioning. | — | rejected | — | — |
| N3-DiffNEA | Sutanto et al. (L4DC 2020) | Online payload adaptation via gradient descent on RNEA inertial parameters — update payload-related link inertias online by backpropagating through the differentiable RNEA. REJECT — subsumed by the existing dual payload-awareness mechanism: `_inject_payload()` in `rnea_wrapper.py` (white-box path) + delta conditioning of tau_res (residual path). Running gradients through RNEA at 1 kHz is computationally incompatible with real-time control. | — | rejected | — | — |

## 3a. Primary baseline — Liu et al. (2024) competitive gaps
Liu et al. (2024): "Physics-Informed Neural Networks to Model and Control Robots:
A Theoretical and Experimental Investigation" — the primary state-of-the-art reference.

| # | Gap in Liu et al. | This project's answer |
|---|-------------------|----------------------|
| 1 | No URDF-to-model automation — equations manually derived per robot | Goal 1: fully automated URDF -> Pinocchio -> network pipeline |
| 2 | Control at 500 Hz only, no MPC integration | Goal 2: targets 1000 Hz; Stage 2 MoveIt2 planning working end-to-end against a simulated Franka (Milestone 1, originally 2026-07-16 on Isaac Sim, re-validated 2026-07-20 on MuJoCo after a Stage 2/3 simulator migration); Stage 3 controller wired into the ROS2 node (2026-07-16, message contract updated for MuJoCo 2026-07-20); Milestone 2 (routing planned trajectories through the trained-model controller) still in progress — a 5th bug (joint-state name-indexing) found and fixed 2026-07-21 (arm now holds cleanly through the mode switch), the earlier `moveit_plan_bridge` planning failure turned out to be a downstream symptom of that bug (now moot), but a further, deeper bug was found the same day: correct time-varying commanded torque produces zero measured motion, root cause not yet confirmed (leading hypothesis: MuJoCo hardware-plugin actuator mode lock) — trajectory tracking has never yet been empirically demonstrated |
| 3 | No payload conditioning | Payload delta in R^1 baked into every network input ([sin(q), cos(q), qdot, delta]); tested at 0 / 1 / 3 kg |
| 4 | No sim-to-real transfer protocol | Goal 4: 2-step pre-train (sim) + fine-tune (real motor-babbling) under PINN loss; `training/fine_tune.py` implements this (N3-Duong) |
| 5 | No torque / velocity limits in loss | AL constraints enforce torque limits and dissipativity for all 7 joints; Stage 3 hard-clips at publish boundary |
| 6 | Full black-box Lagrangian — no RNEA white-box term | Grey-box: RNEA white-box + learned residual; RNEA baseline never modified |

## 3b. Secondary validations from paper reviews (no implementation required)
Independent papers reviewed have produced secondary findings that strengthen existing design choices:

1. **Backlash error is payload-independent and motion-state-dependent** (Hu et al., IEEE/ASME T-Mech 2026): Paper shows backlash manifests as a function of velocity direction and joint position, not payload mass. This passively validates the grey-box decomposition: RNEA correctly accounts for all load-dependent (inertia, Coriolis, gravity) terms, and the learned tau_res cleanly captures the motion-state-dependent residual (including backlash-like phenomena) without entanglement with payload dynamics.

2. **Dual payload-awareness is ahead of the published state of the art** (Li et al., IEEE RA-L 2025): Li et al. propose virtual payload link injection in Pinocchio as a novel contribution. This project already implements exactly this mechanism in `pinocchio_baseline/rnea_wrapper.py` (`_inject_payload()` / `_restore_payload()`), and additionally conditions the learned residual network on the payload scalar delta — providing a second, independent pathway for payload adaptation that Li et al. do not have. No code changes needed; this is a documented advantage over the published baseline.

3. **DeLaN-FFNN grey-box architecture independently validated** (Li et al., 2025 — compliant manipulators): Li et al. apply a DeLaN-style FFNN grey-box (analytical structured terms + neural residual) to compliant robotic manipulators and confirm it outperforms pure black-box models. This is an independent peer-reviewed validation of the grey-box design philosophy used in this project, from a different domain (compliant vs. rigid-body). Useful citation for PAPER_DRAFT.md Section 2 (Related Work).

4. **Broad survey corroborates existing design choices** (Toscano et al., 2025 — PIKANs survey): A comprehensive survey of PINNs vs. PIKANs confirms that grey-box compositions with analytical physics terms, augmented Lagrangian constraint enforcement, and frozen-backbone fine-tuning are recognised best practices in the current state of the art. No architectural changes warranted; serves as a broad citation for PAPER_DRAFT.md Section 2.

5. **DeLaN lineage confirms RNEA-intact superiority and FrictionNet design** (Lutter et al. IROS 2019 + ICLR 2019; Sutanto et al. L4DC 2020 — processed 2026-06-30): The three foundational papers of the Lagrangian-NN / differentiable-RNEA lineage all converge on the same limitation: learning H(q) (DeLaN) or making URDF inertias differentiable (DiffNEA) provides flexibility at the cost of breaking the analytical white-box guarantee. The project's RNEA-intact grey-box approach avoids this trade-off entirely. Secondary findings: (a) Sutanto et al. confirm Pinocchio's suitability for 7-DoF batched RNEA at training time; (b) Sutanto et al. identify a static-friction gap in their DiffNEA that the project's FrictionNet (N2-Liu, MERGED) + augmented Lagrangian dissipativity directly address; (c) Lutter ICLR 2019 is the original source for the Cholesky + Softplus-diagonal design pattern that N2-Liu FrictionNet descends from — cite in PAPER_DRAFT.md Section 2 as the lineage anchor. All three papers are cite-worthy in the Related Work section.

6. **Independent cross-validation of the panda.urdf mass/inertia fix** (2026-07-20, this session): the real Franka Panda link masses/inertias vendored into the MuJoCo MJCF (`mujoco_menagerie`, upstream `franka_description` origin, same as `panda.urdf`'s own values) match the values used in the earlier `panda.urdf` inertial fix (commit c179740). This is a second, independent source confirming that fix was correct — not a new design choice, but strengthens confidence in the Stage 1 URDF baseline for the final paper's methodology section.

## 3c. Physics validator advisories (non-blocking)
1. N4 branch — dissipativity multiplier is batch-mean (pre-existing, not introduced by N4 branch) — consider per-sample multipliers if enforcement is weak at runtime.
2. N4 branch — at very small max_samples (< ~2000 from a 25k dataset), random draw may under-sample high-velocity excitation regions — document this in ablation study.
3. N3-Liu (Stage 3) — DEFAULT_KP/DEFAULT_KD are placeholders computed from `DEFAULT_ERROR_BOUND = [5,5,5,5,2,2,2]` Nm. Gains must be recomputed via `compute_lyapunov_gains(real_error_bound)` once Stage 1 produces validation error statistics. **RESOLVED 2026-07-16** — see Stage 1/Stage 3 sections; a separate fixed `MANUAL_PD_KP`/`MANUAL_PD_KD` fallback was added for the `use_lyapunov_gains=False` path, which previously had no gains to fall back to.
4. N3-Duong (`training/fine_tune.py`) — freeze logic uses `named_modules()` to select "last 2" nn.Linear modules. If a future architecture revision adds skip-connection Linear layers outside `self.net`, the count could shift. Document this assumption when the architecture evolves.
5. N2-Liu FrictionNet — `lambda_dissip` in AugmentedLagrangian will grow more slowly when `--use_friction_net` is active, because FrictionNet structurally absorbs part of the dissipativity constraint. This is intended and correct behaviour; document in ablation comparison between runs with and without the flag.
6. Prabhakar et al. (ICLR 2026) key negative result: physics constraints HURT temporal extrapolation beyond the training window (+6-10% error increase). Scope project claims to in-distribution trajectory following accordingly; do not claim out-of-distribution extrapolation without evidence.
7. **Stage 2/3 simulator migration (2026-07-20):** the earlier reverted Milestone 2 attempt (Isaac Sim) found that Isaac's Franka USD needed manual `set_dof_gains(stiffnesses=0.0, dampings=0.0)` post-`play()` to give effort commands authority over a competing built-in position servo. The new MuJoCo stack sidesteps this entire class of bug by construction — the vendored MJCF actuator is a single direct-torque `motor` per joint with no separate built-in position servo to fight against. Worth noting in the paper's methodology/limitations discussion as a simulator-selection rationale, not just an infrastructure convenience. **Caveat added 2026-07-21 (continued session):** this hoped-for simplicity may be incomplete — the *ROS2 hardware-interface plugin* layer (`mujoco_ros2_control`, separate from the MJCF actuator itself) is now suspected (unconfirmed) of independently locking joints into position-PID mode; see item 9 below. The MJCF-level fix and the plugin-level suspected issue are two different layers of the same stack.
8. **Bug #5 lesson (2026-07-21) — never assume `/joint_states` ordering:** `ros2_control`'s `joint_state_broadcaster` publishes all joints on the controller manager sorted alphabetically, not in URDF/ros2_control-config declaration order, and will silently include unrelated joints (e.g. a mock gripper's finger joints) sharing the same node. Any consumer of `/joint_states` must index by name, never by position — document this as a hard rule for all future ROS2 node code in this project (`pinn_controller_node.py` now follows it via `_arm_indices`; audit any other future subscribers against this rule).
9. **New lesson (2026-07-21, continued session) — check the hardware-interface PLUGIN's actuator mode, not just ROS2-controller-manager state, when commanded torque looks correct but motion is entirely absent.** This session found that `ros2 control list_controllers` reporting `panda_effort_controller` as active (and `panda_arm_controller` as inactive/unloaded) is NOT sufficient evidence that the underlying simulated hardware is actually accepting effort commands: a lower-level hardware-interface plugin (here, the compiled `mujoco_ros2_control` binary) can, in principle, hold a per-joint command-interface mode that is independent of and invisible to controller-manager-level activation state, if the plugin was built to lock interface mode based on static registration-time configuration rather than the standard `perform_command_mode_switch()` runtime hook. This is a general lesson for any project using third-party/binary-only `ros2_control` hardware-interface plugins: when torque/effort commands are demonstrably correct (verified via topic echo) but produce zero physical motion, treat the hardware-interface plugin's actual interface-mode selection (checkable via its own startup log, not just `ros2 control list_controllers`) as a first-class suspect, not just the controller-manager or the control-law code. **Confirmation status: hypothesis only, not yet empirically confirmed** — flagged here for its general debugging-methodology value regardless of how this specific case resolves; strength should be reassessed once the definitive diagnostic (Section 5) is actually run.

## 4. Current repository structure
```
pinn_franka/
|-- goal.md                          4 scientific objectives (north star)
|-- Step1_Indication.md              Full Stage 1 methodology (5-step pipeline)
|-- CLAUDE.md                        Architecture rules and agent permissions
|-- SESSION.md                       Cross-session state (maintained by session-scribe)
|-- requirements.txt                 Python dependencies
|
|-- network/
|   |-- constants.py                 Franka Panda physical constants; FRICTION_NET_HIDDEN=64
|   |-- grey_box_net.py              GreyBoxNet MLP + encode_state(); the learned residual
|   |-- friction_net.py              FrictionNet: 2-layer Mish MLP (22->64->64->7);
|   |                                D_diag = Softplus(d)+1e-6; tau_friction = -D_diag*qdot;
|   |                                hard structural guarantee tau_f·qdot <= 0; 6,087 params
|   |-- __init__.py
|
|-- training/
|   |-- constraints.py               AugmentedLagrangian: torque-limit + dissipativity
|   |-- dataset.py                   SyntheticDataset + FrankaDynamicsDataset (HDF5);
|   |                                _random_subsample_indices() for N4 ablation
|   |-- train.py                     Main training loop (Adam + AL dual ascent);
|   |                                --max_samples CLI arg (N4);
|   |                                --use_friction_net flag (N2-Liu): adds FrictionNet
|   |                                params to optimizer, logs D_diag mean per joint per
|   |                                epoch, saves friction_net_best.pt
|   |-- fine_tune.py                 N3-Duong: frozen-layer fine-tuning on real data;
|   |                                last 2 nn.Linear unfrozen; --max_steps default=100
|   |-- __init__.py
|
|-- pinocchio_baseline/
|   |-- rnea_wrapper.py              RneaBaseline: URDF -> batched RNEA, payload injection
|   |                                (_inject_payload / _restore_payload); ahead of Li 2025;
|   |                                UNCOMMITTED (2026-07-20): __init__ now uses
|   |                                pin.buildReducedModel() to lock finger joints (nq 9->7),
|   |                                required for the live 7-DoF Stage 3 controller
|   |-- __init__.py
|
|-- controller/                      [branch: stage3/computed-torque-pd-controller]
|   |-- __init__.py
|   |-- model_loader.py              Loads GreyBoxNet checkpoint from .pt + config.json
|   |-- lyapunov_gains.py            N3-Liu: Kd = safety_margin*error_bound, Kp = Kd^2/4;
|   |                                MANUAL_ERROR_BOUND + MANUAL_PD_KP/MANUAL_PD_KD fallback
|   |                                gains (safety_margin=1.0); re-reviewed 2026-07-21
|   |                                (continued), still correct, gains confirmed non-negligible
|   |-- computed_torque_pd.py        ComputedTorquePDController.step(); 1 kHz torque kernel;
|   |                                update_payload() for runtime delta changes; wired into
|   |                                pinn_controller_node.py; simulator-agnostic (numpy I/O),
|   |                                unaffected by the Stage 2/3 simulator migration;
|   |                                code-reviewed 2026-07-21 (twice, incl. continued session),
|   |                                no bugs found (confirms bug #5, and separately the new
|   |                                trajectory-tracking-frozen bug, are both upstream/
|   |                                downstream of this class, not in the control law itself)
|   |-- payload_identification.py    TODO stub: least-squares payload ID from static torque
|   |                                measurements; RNEA linearity in mass → 1-unknown
|   |                                closed-form regression; call before 1 kHz loop
|
|-- simulation/                      DEPRECATED for Stage 2/3 as of 2026-07-20 — kept as
|   |                                historical reference, not deleted; Stage 2/3 now uses
|   |                                mujoco_ros2_control (see ros2_ws/ below) instead
|   |-- isaac_franka_moveit_bridge.py  Isaac Sim standalone script: exposes Franka Panda
|                                      over ROS2 topics isaac_joint_states (state, pub) /
|                                      isaac_joint_commands (pos/vel/effort, sub); no longer
|                                      part of the active Stage 2/3 workflow
|
|-- ros2_ws/                         [branch: stage2/moveit2-ros2-humble — distro is
|   |                                  actually ROS2 Jazzy on this machine; simulator
|   |                                  migrated Isaac Sim -> MuJoCo, 2026-07-20, UNCOMMITTED;
|   |                                  2026-07-21: a further pinn_controller_node.py bug fix
|   |                                  (bug #5, joint-state name indexing) lives on a SEPARATE
|   |                                  uncommitted branch fix/jointstate-name-indexing;
|   |                                  2026-07-21 (continued): a SPECULATIVE, UNTESTED
|   |                                  actuator-mode-lock candidate fix lives on a THIRD
|   |                                  uncommitted branch fix/effort-mode-actuator-lock
|   |                                  (stacked on fix/jointstate-name-indexing) — three
|   |                                  branches need reconciling before any merge to main]
|   |-- src/pinn_franka_controller/
|       |-- mujoco/                  NEW this session, untracked
|       |   |-- franka_emika_panda/
|       |       |-- panda_arm_mujoco.xml   Vendored MJCF (mujoco_menagerie, Apache-2.0);
|       |       |                          joints renamed panda_joint1..7; single
|       |       |                          direct-torque `motor` actuator per joint
|       |       |                          (replaces upstream position-servo actuators);
|       |       |                          self-collision excludes match config/panda.srdf;
|       |       |                          hand + finger mass-only welded bodies added
|       |       |                          (fixes a gravity-mismatch bug, earlier session)
|       |       |-- assets/                Vendored meshes (.obj/.stl)
|       |       |-- LICENSE                 mujoco_menagerie Apache-2.0 license
|       |-- urdf/                    NEW this session, untracked
|       |   |-- panda_mujoco.ros2_control.xacro   ros2_control tag block for MuJoCo backend;
|       |   |                                      2026-07-21 (continued, branch
|       |   |                                      fix/effort-mode-actuator-lock, SPECULATIVE/
|       |   |                                      UNTESTED): new arm_control_mode arg
|       |   |                                      (both/effort) to try declaring only the
|       |   |                                      effort command interface per joint
|       |   |-- panda_mujoco.urdf.xacro           Xacro wrapper; same speculative
|       |   |                                      arm_control_mode plumbing as above
|       |   |-- panda_mujoco_description.urdf     Patched moveit_resources_panda_moveit_config
|       |   |                                      URDF with real Franka inertial data added
|       |   |                                      (upstream has zero mass/inertia per link)
|       |-- pinn_controller_node.py  UNCOMMITTED this session: message/topic contract
|       |                            changed to std_msgs/Float64MultiArray on
|       |                            /panda_effort_controller/commands + joint_states
|       |                            (was Isaac's raw sensor_msgs/JointState); torque-
|       |                            computation logic itself unchanged.
|       |                            2026-07-21 (branch fix/jointstate-name-indexing):
|       |                            fixed bug #5 — positional [:7] joint-state slice
|       |                            replaced with name-based _arm_indices lookup;
|       |                            9-joint /joint_states (7 arm + 2 finger) was being
|       |                            scrambled; fix human-verified holding stable through
|       |                            the position->effort switch.
|       |                            2026-07-21 (continued): with bug #5 fixed, a NEW,
|       |                            deeper, UNRESOLVED bug found — controller publishes
|       |                            correct time-varying torque but measured
|       |                            /joint_states position never changes at all during
|       |                            a tracking attempt (bit-for-bit frozen). Root cause
|       |                            suspected (unconfirmed) to be outside this file, in
|       |                            the mujoco_ros2_control hardware-interface plugin.
|       |-- moveit_plan_bridge.py    NEW this session, untracked (restored from a prior
|       |                            session's reverted Milestone 2 attempt): raw
|       |                            moveit_msgs/action/MoveGroup plan_only=True client,
|       |                            republishes planned trajectory to
|       |                            /pinn_controller/desired_trajectory; pub/shutdown DDS
|       |                            race fixed in an earlier session. 2026-07-21: an
|       |                            error_code=99999 planning failure was seen for at
|       |                            least one target, then (continued, same day)
|       |                            RESOLVED/MOOT — was a downstream symptom of bug #5's
|       |                            corrupted arm start-state, not an independent bug;
|       |                            replans successfully now. No open item on this file.
|       |-- trajectory_interpolator.py  Linear interp of (q_des, qdot_des, qddot_des);
|       |                            unchanged this session; re-reviewed 2026-07-21
|       |                            (continued), still correct
|       |-- launch/
|       |   |-- pinn_controller.launch.py     Unchanged this session (last touched 2026-07-16)
|       |   |-- mujoco_franka_moveit.launch.py  NEW this session, untracked: replaces the
|       |   |                                    external isaac_moveit.launch.py dependency;
|       |   |                                    2026-07-21 (continued, branch
|       |   |                                    fix/effort-mode-actuator-lock, SPECULATIVE/
|       |   |                                    UNTESTED): new arm_control_mode launch arg
|       |   |                                    (both/effort); in effort mode, Milestone 1's
|       |   |                                    panda_arm_controller cannot be spawned in
|       |   |                                    the same launch (no position interface to
|       |   |                                    claim) — mutually exclusive with both mode
|       |-- config/
|           |-- controller_params.yaml         Unchanged this session
|           |-- mujoco_effort_controller.yaml  NEW this session, untracked: panda_effort_controller
|           |-- mujoco_pid.yaml                NEW this session, untracked: internal PID gains
|
|-- stage4/                          [merged to main: commit b86f335]
|   |-- grasp_config.py              GraspConfig dataclass: all grasping parameters
|   |-- gripper_controller.py        BaseGripperController ABC; FrankaROS2GripperController
|   |                                (ROS2 Humble action clients); MockGripperController
|   |-- grasp_executor.py            GraspExecutor state machine: pick()/place(), 8 phases;
|   |                                calls arm.update_payload() after successful grasp;
|   |                                _move_arm() still NotImplementedError — one step closer
|   |                                after Stage 2/3 Milestone 1, still blocked on Milestone 2
|   |-- demo_targets.py              Named poses + orientations; get_target() -> (4,4) SE3
|   |-- dry_run.py                   6 offline tests; run: python -m stage4.dry_run
|
|-- tracking/
|   |-- PROJECT_STATE.md             This file
|   |-- papers_review.csv            One row per paper processed
|
|-- papers/
|   |-- inbox/                       EMPTY — 3 unacquired papers remain outside repo
|   |                                (s11433-025-2810-1, s41598-026-50630-y_reference,
|   |                                ssrn-6550385); refill manually when obtained
|   |-- processed/
|       |-- Liu et al. (2024)...     (primary baseline, see section 3a)
|       |-- Djeumou et al. (2022)... (processed)
|       |-- Duong et al. (2024)...   (processed)
|       |-- Wang et al. (CAC 2024)   (processed — Trajectory Control / NMPC)
|       |-- Wang et al. (2025)...    (processed — SPEL)
|       |-- Prabhakar et al. (2026). (processed — "When Does Physics Help?", ICLR 2026; relevance 1; N2-WhenPhysics INVESTIGATE)
|       |-- Hu et al. (2026)...      (processed — Backlash PINN, IEEE/ASME T-Mech; relevance 1, all REJECTed; secondary finding: backlash payload-independence validates RNEA decomposition)
|       |-- Li et al. (2025)...      (processed — Payload ID PINN, IEEE RA-L; relevance 1, all REJECTed; secondary finding: dual payload-awareness already implemented, ahead of paper)
|       |-- Li et al. (2025)...      (processed — compliant manipulator PINN, DeLaN-FFNN; relevance 2, N1-CompliantPINN + N2-CompliantPINN REJECTed; secondary finding: independent validation of grey-box design; cite in Related Work Section 2)
|       |-- Toscano et al. (2025)... (processed — PINNs to PIKANs survey; relevance 1, N1-PIKANs + N2-SNRDiag REJECTed; secondary finding: survey corroborates grey-box + AL + frozen-backbone design; cite in Related Work Section 2)
|       |-- Lutter, Listmann, Peters (IROS 2019)... (processed — DeLaN for energy-based control of under-actuated systems; relevance 1, all REJECTed; bibliographic value: foundational DeLaN predecessor, cite as origin of Lagrangian NN lineage)
|       |-- Lutter, Ritter, Peters (ICLR 2019)...  (processed — Deep Lagrangian Networks seminal paper; relevance 1, all REJECTed; bibliographic value: original source for Cholesky+Softplus-diagonal PD-matrix design; cite in Related Work Section 2)
|       |-- Sutanto et al. (L4DC 2020)...          (processed — Differentiable Newton-Euler Algorithm; relevance 1, all REJECTed; secondary findings: Pinocchio suitability for 7-DoF confirmed; static friction gap in DiffNEA validates FrictionNet + AL design)
|
|-- docs/
|   |-- HOW_TO_USE.md
|   |-- STAGE1_README.md
|
|-- .claude/
    |-- agents/                      Agent role definitions
    |-- commands/process-papers.md   /process-papers workflow
    |-- skills/paper-review-format.md
    |-- hooks/                       block-python-run.sh + session-end-reminder.sh
```

## 5. Open items / next steps

### Done this session (2026-07-21, Milestone 2 debugging, CONTINUED — trajectory tracking attempt)
- [x] **`moveit_plan_bridge`'s `error_code=99999` planning failure RESOLVED/MOOT** — replanned successfully once retried after the bug #5 joint-indexing fix and a clean pose reset. Confirmed to be a downstream symptom of the arm's corrupted start-state from the already-fixed bug, not an independent bug. No longer an open item.
- [x] **First real trajectory-tracking test attempted** — with the arm holding cleanly (bug #5 fixed) and planning working (moot blocker above), ran a genuine tracking test (arm following a moving target, not just holding).
- [x] **Confirmed the controller itself is working correctly**: captured real, substantial, correctly time-varying commanded torque during the tracking window (joint1 ~0 -> 38.65 Nm, joint4 22.17 -> 29.44 Nm) — this is not a stuck/frozen/near-zero computation.
- [x] **Found the arm's measured position never changes**: `/joint_states` position is bit-for-bit frozen (min==max to full float64 precision) across a 4-second capture window overlapping the tracking attempt, for all 7 arm joints. This is a hard numeric result, not a visual impression.
- [x] **Ruled out (empirically, live tests) as candidate causes**: (a) `panda_effort_controller` not active — refuted via `ros2 control list_controllers`; (b) MuJoCo-internal stale position-PID via the shared actuator — refuted, persisted even after full `unload` of `panda_arm_controller`; (c) MuJoCo viewer local pause state — refuted, spacebar toggle showed "PAUSED" appearing, i.e. was running beforehand; (d) trajectory duration exceeding the 2.0 s staleness window — refuted, actual duration ~1.13 s via live topic echo; (e) control-law/gain/interpolator bugs — refuted, `computed_torque_pd.py`, `lyapunov_gains.py`, `trajectory_interpolator.py` all re-reviewed, still correct.
- [x] **Identified leading (unconfirmed) hypothesis**: the compiled `mujoco_ros2_control` plugin binary may lock each joint's actuator into position-PID mode independent of ROS2-controller-manager state, based on `strings`-recovered per-joint mode-selection log strings and the frozen pose matching `initial_positions.yaml` — in unreconciled tension with an earlier reading of the plugin's actual upstream source (which described mode as dynamically switched at runtime).
- [x] **Speculatively applied an untested candidate fix** on new branch `fix/effort-mode-actuator-lock` (stacked on `fix/jointstate-name-indexing`): new `arm_control_mode` launch/xacro argument (`both`/`effort`).
- [ ] **NOT YET RUN — the definitive diagnostic**: check `ros2_control_node`'s startup log for per-joint position-vs-effort-enabled interface messages. This is free, non-destructive, and must be the first action next session, before trusting or discarding the leading hypothesis or the untested fix.
- [ ] **Milestone 2 STILL NOT CLOSED** — trajectory tracking has never once been empirically confirmed working at any point in this project's history.
- [ ] **NOT YET COMMITTED** — now THREE overlapping uncommitted branches touching overlapping files: the 2026-07-20 MuJoCo-migration work, `fix/jointstate-name-indexing` (bug #5, confirmed working), and `fix/effort-mode-actuator-lock` (speculative, untested). Reconcile before any merge.

### Immediate next step — Stage 2/3 Milestone 2 (in progress, resume here)
- **FIRST ACTION: run the definitive diagnostic** — check the `ros2_control_node` startup log/scrollback for per-joint "position control enabled (velocity, effort disabled)" vs "effort control enabled" messages for `panda_joint1`-`panda_joint7`. This confirms or refutes the actuator-mode-lock hypothesis before anything else is attempted.
- **If confirmed:** test the speculative `arm_control_mode:=effort` fix on branch `fix/effort-mode-actuator-lock`; if it produces real motion, promote it from speculative to validated, and document the Milestone 1 / Milestone 2 mutual-exclusivity tradeoff clearly (see Stage 2 section).
- **If refuted:** the actuator-mode-lock hypothesis is wrong; re-open the investigation — re-examine `mujoco_ros2_control`'s actual runtime `perform_command_mode_switch()` behavior (already read once this session, in tension with the `strings` evidence) and consider other layers between the published `/panda_effort_controller/commands` topic and MuJoCo's actual actuator application.
- **Once trajectory tracking is confirmed working**: complete at least one full test cycle (idle hold -> triggered motion -> post-trajectory idle hold), watching for genuine, stable, non-violent joint motion during the trajectory window, before declaring Milestone 2 done.
- **Reconcile THREE branches before committing**: `fix/jointstate-name-indexing` (bug #5, confirmed working), `fix/effort-mode-actuator-lock` (speculative, untested, stacked on the former), and the original 2026-07-20 Stage 2/3 MuJoCo-migration work — all uncommitted, all overlapping on shared files. Commit in a clear sequence once Milestone 2 is fully validated (do not merge the speculative branch's changes if the diagnostic refutes the hypothesis).
- Once Milestone 2 is done, revisit `stage4/grasp_executor.py`'s `_move_arm()` — it can then be implemented against the real Stage 2/3 control path instead of remaining a `NotImplementedError` stub.

### Done this session (2026-07-21, Milestone 2 debugging, earlier — joint-indexing fix)
- [x] **Found and fixed bug #5**: `pinn_controller_node.py` positional `[:7]` joint-state slicing was scrambling q_meas/qdot_meas against a 9-joint `/joint_states` topic (7 arm + 2 mock-gripper fingers, alphabetically sorted) — 100% reproducible cause of the arm collapsing on every position->effort controller switch. Fixed via a name-based `_arm_indices` lookup helper. Root-caused via live testing + upstream source reading (`mujoco_ros2_control`, `ros2_controllers`), confirmed via direct `/joint_states` inspection.
- [x] **Fix human-verified**: arm now holds cleanly through the position->effort switch, directly observed in the MuJoCo viewer. Ruled out three alternative hypotheses (switch sequencing, MuJoCo internal PID fighting effort commands, ForwardCommandController buffer reset) before finding the real cause.
- [x] **Code review**: re-confirmed `ComputedTorquePDController.step()` and `TrajectoryInterpolator` are logically correct — bug #5 was a data-plumbing issue upstream of the control law, not a bug in the control law itself.

### Done this session (2026-07-20, Stage 2/3 simulator migration)
- [x] **Migrated Stage 2/3's simulator from Isaac Sim to MuJoCo** (`mujoco_ros2_control`, apt-installable `ros-jazzy-mujoco-ros2-control`) — removes the external `~/IsaacSim-ros_workspaces` submodule/build dependency for Stage 2/3 entirely. Stage 1 (data generation/training) is untouched and continues on Isaac Sim.
- [x] **Vendored Franka Panda MJCF + meshes from `mujoco_menagerie`** (`ros2_ws/src/pinn_franka_controller/mujoco/franka_emika_panda/`), patched for project joint naming, single direct-torque actuator per joint, and self-collision excludes matching `config/panda.srdf`.
- [x] **New patched URDF with real inertials** (`ros2_ws/src/pinn_franka_controller/urdf/`) — required because `moveit_resources_panda_moveit_config`'s upstream URDF is kinematics-only (zero mass/inertia), insufficient for MuJoCo's dynamics compiler.
- [x] **New self-contained launch file** `launch/mujoco_franka_moveit.launch.py`, replacing the external `isaac_moveit.launch.py` dependency.
- [x] **New MuJoCo controller configs** `config/mujoco_effort_controller.yaml` (Stage 3 effort path) and `config/mujoco_pid.yaml` (MoveIt2 default position-mode path), both driving the same single per-joint torque actuator.
- [x] **`pinn_controller_node.py` message/topic contract updated** for `mujoco_ros2_control`/`ros2_control` conventions: `std_msgs/Float64MultiArray` on `/panda_effort_controller/commands` + `joint_states`, replacing Isaac's raw `sensor_msgs/JointState` on `isaac_joint_commands`/`isaac_joint_states`. Torque-computation logic itself unchanged.
- [x] **`pinocchio_baseline/rnea_wrapper.py` `RneaBaseline.__init__` fixed**: added `pin.buildReducedModel()` to lock finger joints (nq 9 -> 7) — a real, previously-latent bug required for any live 7-DoF controller run regardless of simulator.
- [x] **`moveit_plan_bridge.py` restored** (originally written during the prior session's reverted Isaac Sim Milestone 2 attempt; the code itself was simulator-agnostic).
- [x] **`simulation/isaac_franka_moveit_bridge.py` kept, not deleted** — retained as deprecated/historical reference; no longer part of the active workflow.
- [x] **Milestone 1 re-validated and STABLE on MuJoCo**: MoveIt2 Plan & Execute in RViz correctly drives the simulated Franka Panda; a live position-hold oscillation was found and fixed, traced to the inherited demo `ros2_control` loop running at 100 Hz instead of the project's 1000 Hz target.
- [x] **Independent cross-validation of the Stage 1 URDF mass/inertia fix**: `mujoco_menagerie`'s real Franka masses/inertias (same `franka_description` origin) match `panda.urdf`'s already-fixed values.
- [ ] **Milestone 2 IN PROGRESS** — continued 2026-07-21 (bug #5 found/fixed, moveit_plan_bridge blocker resolved/moot, then a new deeper trajectory-tracking-frozen bug found), still not complete — see "Immediate next step" above.
- [ ] **NOT YET COMMITTED** — all of the above is still in the working tree: modified `pinocchio_baseline/rnea_wrapper.py`, `ros2_ws/src/pinn_franka_controller/package.xml`, `ros2_ws/src/pinn_franka_controller/pinn_franka_controller/pinn_controller_node.py`, `ros2_ws/src/pinn_franka_controller/setup.py`, `simulation/isaac_franka_moveit_bridge.py`; new untracked `ros2_ws/src/pinn_franka_controller/config/mujoco_effort_controller.yaml`, `ros2_ws/src/pinn_franka_controller/config/mujoco_pid.yaml`, `ros2_ws/src/pinn_franka_controller/launch/mujoco_franka_moveit.launch.py`, `ros2_ws/src/pinn_franka_controller/mujoco/`, `ros2_ws/src/pinn_franka_controller/pinn_franka_controller/moveit_plan_bridge.py`, `ros2_ws/src/pinn_franka_controller/urdf/`. Commit and push before continuing Stage 2/3 work.

### Done since prior update (2026-07-16, Stage 2/3 integration + Stage 1 sessions)
- [x] **Stage 2 first working implementation** (superseded 2026-07-20 by the MuJoCo migration above; historical) — MoveIt2 motion planning against a simulated Franka Panda in Isaac Sim via NVIDIA's `IsaacSim-ros_workspaces` (`jazzy_ws/isaac_moveit`).
- [x] **Stage 3 `ComputedTorquePDController` wired into `pinn_controller_node.py` for the first time** — the previous `TODO(stage3)` zero-torque stub is resolved; the node now actually calls `.step()` per control tick.
- [x] **`controller/lyapunov_gains.py`: added `MANUAL_PD_KP`/`MANUAL_PD_KD` fallback gains** (fixed conservative error bound, safety_margin=1.0) for the node's `use_lyapunov_gains=False` path, which previously had no effect.
- [x] **`pinocchio_baseline/panda.urdf` mass/inertia fix** — added real Franka `franka_description` inertials to every link; RNEA was previously running on Pinocchio placeholder inertias. Commit `c179740`, pushed to `origin/main`. **Independently cross-validated 2026-07-20** via the vendored MuJoCo MJCF (see Section 3b item 6).
- [x] **Isaac Sim 6.0.1 data pipeline validated end-to-end** — namespace migration (`isaacsim.*`), pinocchio/cmeel import fix, Franka USD asset path, `TORQUE_LIMITS` dtype, `world.stop()` API, RNEA reduced-model (locking finger joints), tracking-error + drive-gain diagnostics. Same commit as above.
- [x] **First two real GPU training runs completed and logged** (`tracking/experiments_log.csv`): initial run found an actuator-saturation data-quality bug via a newly-written diagnostic tool (`controller/compute_error_bound.py`); fixed in `generate_isaac_dataset.py` (`SATURATION_MARGIN = 0.97`); re-generated + retrained. `isaac-multipayload-frictionnet-satfix` is now the reference baseline (best val loss 0.3995, see Stage 1 section for full writeup).
- [x] **Stage 3 Lyapunov gains recomputed from real data** — `DEFAULT_ERROR_BOUND` in `controller/lyapunov_gains.py` replaced with real per-joint p99.9 validation error, no longer a `[5,5,5,5,2,2,2]` Nm placeholder. Methodology (p99.9 vs. max, and why) documented in code comments and PAPER_DRAFT.md §3.6.
- [x] `experiments/EXPERIMENT_PLAN.md` Block G extended with a fine-grained payload-interpolation test (eval-only 0.5/1.5/2.5 kg against the {0,1,3} kg trained model).
- [x] PAPER_DRAFT.md updated: URDF-fix methodological note (§3.2), Lyapunov error-bound methodology (§3.6), new §4.3 "First GPU Training Results" documenting the saturation-fix before/after comparison.

### Blocked on GPU
- None — GPU access is unblocked and in active use on the `hci-student` machine.

### Not yet done (successor to the old "blocked on GPU" items)
- **Full ablation matrix** (`experiments/EXPERIMENT_PLAN.md` blocks A-I, headline H1-H4) — plan exists, CLI flags for new ablation axes (structure variants, diagonal-vs-Cholesky, activation swap, constraint mode) not yet implemented. Redirected in favor of Stage 2/3 integration work; resume once Milestone 2 is done or in parallel.
- **Liu et al. (2024) 2s-rollout metric** — required for the external comparison claim, not yet implemented (per-joint RMSE in Nm alone is not comparable to their rad²/rad metric).
- **Remaining J1-J4 transient-spike outliers** (p99.9 3-6 Nm, true max 26-65 Nm on ~0.09% of validation samples) — not blocking, but root cause (suspected: PD servo snapping to the first velocity target at each Fourier segment start) not yet confirmed or fixed. Revisit if time permits; currently worked around via the p99.9-not-max Lyapunov bound methodology.
- **Re-run one prior CPU smoke-test experiment** on the corrected URDF for a clean before/after comparison at matched settings (optional — the two real GPU runs already provide a clean before/after story for the saturation fix specifically).

### Blocked on real robot data
- Build real motor-babbling HDF5 dataset to enable `training/fine_tune.py` (Goal 4): `python -m training.fine_tune --checkpoint models/run_XXXX/greybox_best.pt --real_data data/real_motor_babbling.h5 --max_steps 100`
- N3-Djeumou (semi-supervised dissipativity): design and run ablation study.
- N2-WhenPhysics: re-run diagnostic on real data — real per-joint friction differences may trigger EMA balancing (ratio 1.0× on synthetic, may differ on hardware).

### Stage wiring (pending Milestone 2 completion)
- ~~Resolve `TODO(stage3)` markers in `pinn_controller_node.py`~~ **DONE 2026-07-16** — `ComputedTorquePDController` now wired in; see Stage 3 section. Remaining: exercise this wiring end-to-end via Milestone 2 (routing MoveIt2 trajectories through this node rather than the default position-mode controller) — in progress on MuJoCo; bug #5 (joint-state indexing) found and fixed 2026-07-21, `moveit_plan_bridge` blocker resolved/moot the same day, but a new, deeper, unresolved trajectory-tracking-frozen bug was then found — see "Immediate next step" above.
- Resolve `TODO(stage4-arm-motion)` in `stage4/grasp_executor.py` (`_move_arm()`) — one step closer after Milestone 1, still blocked on Milestone 2.
- **Inference latency benchmark:** add a timing script to confirm sub-1 ms per forward pass (required to substantiate Goal 2 claim at 1000 Hz). Still not started; more relevant now that Stage 3 is actually wired into the control loop and the 100 Hz vs 1000 Hz `ros2_control` loop-rate issue has surfaced (see Section 3c item 7 / Objective 2 evidence).

### Paper / write-up
- **Add Related Work citations** (PAPER_DRAFT.md Section 2):
  - Lutter et al. (ICLR 2019) — original Cholesky+Softplus-diagonal source; lineage anchor for FrictionNet design.
  - Lutter et al. (IROS 2019) — foundational DeLaN predecessor for energy-based control lineage.
  - Sutanto et al. (L4DC 2020) — DiffNEA; confirm RNEA-intact advantage and FrictionNet static-friction motivation.
  - Li et al. (2025) compliant PINN — DeLaN-FFNN independent validation.
  - Toscano et al. (2025) PIKANs survey — broad corroboration of grey-box + AL + frozen-backbone.
- **Dual payload-awareness competitive advantage:** document in paper that `_inject_payload()` in `rnea_wrapper.py` + delta conditioning of tau_res predates and exceeds Li et al. (2025) payload-ID and Li et al. (2025) compliant PINN single-pathway approaches.
- **Scope physics claims to in-distribution trajectories:** following Prabhakar et al. (ICLR 2026) negative result — do not claim temporal extrapolation without empirical evidence.
- **Physics-validator advisories to address in paper methodology section:** (1) per-sample vs. batch-mean dissipativity multiplier; (2) under-sampling risk for max_samples < 2000; (3) slower lambda_dissip growth with FrictionNet (intended — document in ablation); (4) simulator-selection rationale (MuJoCo's single direct-torque actuator vs. Isaac Sim's competing built-in position servo, Section 3c item 7); (5) `/joint_states` ordering pitfall (Section 3c item 8) as a general implementation lesson for reproducibility; (6) binary/closed-source `ros2_control` hardware-interface plugins as a debugging-opacity risk, and the "check the plugin's own interface-mode logs, not just controller-manager state" lesson (Section 3c item 9), once its underlying hypothesis is confirmed or refuted.
- **Document the Stage 2/3 simulator migration decision** (Isaac Sim -> MuJoCo, 2026-07-20): justify in the methodology section as removing an external, unversioned build dependency (`~/IsaacSim-ros_workspaces`) in favor of an apt-installable ROS2 package, and as avoiding a class of built-in-servo-vs-effort-command conflict bugs by construction — while explicitly noting Stage 1's data (the scientific core of the project) is untouched and remains Isaac Sim-sourced, AND (2026-07-21 addition) honestly noting the tradeoff that the apt-installable package's compiled-binary nature has itself become a real debugging obstacle for the trajectory-tracking-frozen bug.
- **Document the earlier Stage 2 simplification decision:** using an official/vendored MoveIt2 config (originally NVIDIA's `isaac_moveit`, now the patched `moveit_resources_panda_moveit_config`-derived URDF for MuJoCo) instead of a hand-authored one from scratch — justify as a deliberate scope decision (CLAUDE.md "keep it simple") rather than an oversight.

### Future (optional)
- **Implement `controller/payload_identification.py`**: fill in the 3-step least-squares body (replace `raise NotImplementedError`). ~10 lines numpy. Requires Franka joint-torque sensor access (libfranka or ROS2 `/joint_states` effort field). Priority: before first real-robot deployment.
- 3 papers not yet obtained: s11433-025-2810-1, s41598-026-50630-y_reference, ssrn-6550385.
- Real motor-babbling HDF5 recording (needed for N3-Duong fine-tuning and N3-Djeumou ablation).
</content>
